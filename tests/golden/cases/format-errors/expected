$ krowk push --format human
stderr:
✗ Pass at least one path.
  (no_file)
  try:  krowk push screenshot.png
exit: 1

$ krowk push --format markdown
stderr:
✗ Pass at least one path.
  (no_file)
  try:  krowk push screenshot.png
exit: 1

$ krowk push --format url
stderr:
✗ Pass at least one path.
  (no_file)
  try:  krowk push screenshot.png
exit: 1

$ krowk bogus --format human
stderr:
✗ `bogus` is not a krowk command.
  (unknown_command)
  try:  krowk --help
exit: 1

