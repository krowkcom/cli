$ krowk config set
stderr:
{
  "ok": false,
  "error": {
    "error": "missing_argument",
    "fix": "pass the key and the value: `krowk config set workspace <name>` — keys: workspace",
    "retryable": false
  }
}
exit: 1

$ krowk config set onlykey
stderr:
{
  "ok": false,
  "error": {
    "error": "missing_argument",
    "fix": "pass the key and the value: `krowk config set workspace <name>` — keys: workspace",
    "retryable": false
  }
}
exit: 1

$ krowk config unset
stderr:
{
  "ok": false,
  "error": {
    "error": "missing_argument",
    "fix": "pass the key: `krowk config unset workspace` — keys: workspace",
    "retryable": false
  }
}
exit: 1

$ krowk config show extra
stdout:
{
  "ok": true,
  "data": {
    "global_path": "<scratch>/home/.config/krowk/config.json"
  },
  "summary": "no configuration set — uploads use the stored default key",
  "breadcrumbs": [
    {
      "action": "set the workspace",
      "cmd": "krowk config set workspace <workspace>",
      "description": "pins which workspace commands in this repository use — --global sets the machine-wide fallback instead"
    }
  ]
}
exit: 0

