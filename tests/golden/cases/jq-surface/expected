$ krowk help --jq '.name'
stdout:
krowk
exit: 0

$ krowk help --jq '[.commands[].name] | @csv'
stdout:
"push","uploads","runs","claim","auth","workspaces","config","doctor","sessions","pricing","upgrade","help"
exit: 0

$ krowk help --jq '[.commands[].name] | @tsv'
stdout:
push	uploads	runs	claim	auth	workspaces	config	doctor	sessions	pricing	upgrade	help
exit: 0

$ krowk help --jq '[.global_flags[] | [.name, .type]] | .[] | @csv'
stdout:
"workspace","string"
"dev","bool"
"format","string"
"json","bool"
"quiet","bool"
"jq","string"
"help","bool"
"version","bool"
exit: 0

$ krowk help --jq '.environment[0] | to_entries[0].key'
stdout:
name
exit: 0

$ krowk help --jq '.environment[0] | keys'
stdout:
["name","usage"]
exit: 0

$ krowk help --jq '.environment[0] | [.[] | type]'
stdout:
["string","string"]
exit: 0

$ krowk help --jq '[.environment[0] | paths]'
stdout:
[["name"],["usage"]]
exit: 0

$ krowk help --jq '.environment[0] | tojson'
stdout:
{"name":"KROWK_TOKEN","usage":"API token — wins over the credentials file"}
exit: 0

$ krowk help --jq '[.commands[0].args[0] | tostream]'
stdout:
[[["name"],"file"],[["repeated"],true],[["required"],true],[["summary"],"Path to upload"],[["summary"]]]
exit: 0

$ krowk help --jq '[nan, infinite, -infinite, 1.5e300 * 1e10]'
stdout:
[null,1.7976931348623157e+308,-1.7976931348623157e+308,1.7976931348623157e+308]
exit: 0

$ krowk help --jq '.commands | length'
stdout:
12
exit: 0

$ krowk help --jq '[.commands[] | select(.subcommands) | .name]'
stdout:
["uploads","runs","auth","workspaces","config","sessions","pricing"]
exit: 0

$ krowk help --jq '.commands[0].name | ascii_upcase'
stdout:
PUSH
exit: 0

$ krowk help --jq '.commands[0].name | test("pu")'
stdout:
true
exit: 0

$ krowk help --jq '.name | @base64'
stdout:
a3Jvd2s=
exit: 0

$ krowk help --jq '[limit(2; .commands[].name)]'
stdout:
["push","uploads"]
exit: 0

$ krowk help --jq '.commands[0] | has("usage")'
stdout:
true
exit: 0

$ krowk help --jq '.name | tonumber'
stderr:
{
  "ok": false,
  "error": {
    "error": "jq_failed",
    "fix": "--jq: <jq error>. The command itself succeeded — this is the reading of its result failing, so running it again repeats whatever it did",
    "retryable": false
  }
}
exit: 1

$ krowk help push --jq '.flags | map(.name) | join(",")'
stdout:
run,caption,destination,private,pull-request,link,link-title,link-rel,reference,session,title,repo,commit,agent,metadata
exit: 0

$ krowk help --jq 'halt_error'
stderr:
{
  "ok": false,
  "error": {
    "error": "jq_failed",
    "fix": "--jq: halt and halt_error are not honoured — in jq they choose the process's exit code, and krowk's exit codes are what happened to the artifact. The command itself succeeded — this is the reading of its result failing, so running it again repeats whatever it did",
    "retryable": false
  }
}
exit: 1

